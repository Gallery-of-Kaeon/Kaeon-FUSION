Zero

	Philosophy
	
		-
			Zero is a file format for encoding a ONE document in a way that optimizes the speed at
			which data can be extracted and encoded.
			
			If used as an alternate syntax for ONE+, it will have the alias "Zero".
		-