To Bytes

	Philosophy

		-
			The to bytes command is a command with the content "To Bytes". It has one child that
			returns an assembly dialect document in ONE list form.

			It serializes the returned document into a byte array and then returns said byte array.
		-