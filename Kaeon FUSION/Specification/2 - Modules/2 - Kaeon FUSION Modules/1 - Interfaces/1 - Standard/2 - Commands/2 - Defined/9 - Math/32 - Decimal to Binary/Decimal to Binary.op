Decimal to Binary

	Philosophy
	
		-
			The decimal to binary command contains the content "Decimal to Binary". It must have a
			single child that returns a number.
			
			It returns a string containing the binary representation of the returned number.
		-