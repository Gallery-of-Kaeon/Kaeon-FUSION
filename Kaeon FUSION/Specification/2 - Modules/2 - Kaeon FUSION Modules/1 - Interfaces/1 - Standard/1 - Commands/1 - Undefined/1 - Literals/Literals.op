Literals
	
	Philosophy
		
		-
			A command with content that does not match the name of a defined command or any alias
			in the state and has no children will return its content as a string. If its content
			begins and ends with a double quote, the double quotes will be cropped from the
			returned content.
		-