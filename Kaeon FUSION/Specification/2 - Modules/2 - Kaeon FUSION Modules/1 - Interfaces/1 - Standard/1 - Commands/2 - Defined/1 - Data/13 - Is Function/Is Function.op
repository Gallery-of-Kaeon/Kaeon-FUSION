Is Function

	Philosophy
	
		-
			The is function command contains that content "Is Function". It has one child that
			returns a string.
			
			It will return true if there is a function alias in the state that matches the returned
			string.
		-