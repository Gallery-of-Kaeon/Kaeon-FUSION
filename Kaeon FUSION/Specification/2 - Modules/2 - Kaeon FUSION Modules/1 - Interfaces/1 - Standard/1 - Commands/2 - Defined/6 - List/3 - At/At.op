At
	
	Philosophy
		
		-
			The at command contains the content "At" It has two children, the first of which
			returns a list, and the second of which returns an integer.
			
			It returns the object in the list returned by the first child at the index specified by
			the integer returned by the second child.
		-