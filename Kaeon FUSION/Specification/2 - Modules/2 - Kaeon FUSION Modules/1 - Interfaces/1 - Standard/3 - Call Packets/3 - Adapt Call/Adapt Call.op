Adapt Call

	Philosophy
	
		-
			An adapt call is a type of packet sent through a call command that is used to tie the
			stored namespace of a standard interface function to an object within the host
			environment.
			
			It shall consist of the string "Adapt", followed by the stored namespace to be used,
			followed by a string specifying the type of object being adapted.
		-