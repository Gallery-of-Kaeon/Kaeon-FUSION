Insert
	
	Philosophy
		
		-
			The Insert command contains the content "Insert". It has three children, the first of
			which returns a list, the second of which returns an integer, and the third of which
			returns an object.
			
			It will insert the the object returned by the third child into the list returned by the
			first child at the index specified by the integer returned by the second child.
		-