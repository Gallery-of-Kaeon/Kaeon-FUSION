Concatenate
	
	Philosophy
		
		-
			The concatenate command contains the content "Concatenate".
			
			It will convert each object returned by its children to a string, concatenate said
			strings into a single string, and return the concatenated string.
		-