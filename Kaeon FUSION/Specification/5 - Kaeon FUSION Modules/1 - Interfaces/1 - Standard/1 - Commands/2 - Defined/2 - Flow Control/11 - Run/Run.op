Run
	
	Philosophy
		
		-
			The run command contains the content "Run" and may have an indefinite number of
			children, each of which return a string.
			
			It runs each returned string as a command line call.
		-