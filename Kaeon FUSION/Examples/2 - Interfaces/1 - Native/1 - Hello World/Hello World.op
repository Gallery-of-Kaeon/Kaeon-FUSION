#[

	This code use's Kaeon FUSION's Machine Interface to cross compile a Kaeon FUSION definition to
	C code.
	
	Running this code will produce a file called "main.c", which will contain the following C code:

	#include <stdio.h>

	int main() {
		printf("Hello, world")
		return 0;
	}

 ]#

Define: Meta { Used } printf

Define: Meta { Integer } main
	
	Use: stdio.h
	
	printf: "Hello, world!"
	Return: 0

Build Native Code: main