Character
	
	Overview
		
		-
			The character command contains the content "Character", and has two children. The first
			child returns a string and the second child returns an integer. The command returns the
			character in the string at the index specified by the integer. The indexes start at
			one.
		-