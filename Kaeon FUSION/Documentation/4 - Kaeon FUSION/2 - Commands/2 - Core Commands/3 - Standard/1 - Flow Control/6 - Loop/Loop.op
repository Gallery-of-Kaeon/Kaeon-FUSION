Loop
	
	Overview
		
		-
			The loop command contains the content "Loop".
			
			The loop command may optionally have a child that returns a boolean value. If the
			value returned is true, or if the command has no children, it will shift the Kaeon
			FUSION script to the first child of its parent.
		-