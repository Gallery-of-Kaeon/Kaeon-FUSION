Call
	
	Overview
		
		-
			The call command contains the content "Call" and may have an indefinite number of
			children. It calls the host environment with a packet containing all of the objects
			returned by its children in order, and returns a list of all objects returned to it by
			the call.
		-