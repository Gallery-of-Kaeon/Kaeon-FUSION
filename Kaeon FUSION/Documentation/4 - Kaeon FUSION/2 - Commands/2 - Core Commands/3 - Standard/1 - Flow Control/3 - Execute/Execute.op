Execute
	
	Overview
		
		-
			The execute command contains the content "Execute", has a child that either returns a
			string, or has content that matches the name of a function. If the child returned a
			string, the returned string will be executed as Kaeon FUSION code. If the child
			contains the name of a function as content, the code within the function will be
			executed in the current namespace.
		-